name: fluxon_method_generator
description: Code generator for fluxon service contracts (clients/dispatchers)
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  analyzer: ^8.0.0
  build: ^4.0.0
  source_gen: ^4.0.0

dev_dependencies:
  build_runner: ^2.8.0

publish_to: none

